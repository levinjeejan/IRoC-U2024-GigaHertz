# Algorithm for Thermal Control Subsystem
```python
while( true/ working){
     check temperature sensors , and return output sequence, use output sequence to identify out of limit temperature
     if temp > limit 
              execute temp reduction strategies depending on the module.
               -- pause for all rover movement related activities
               -- for other modules suitable strategies
```
